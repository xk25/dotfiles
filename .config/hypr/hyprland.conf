# autogenerated = 1 # remove this line to remove the warning

# This is an example Hyprland config file.
# Refer to the wiki for more information.
# https://wiki.hyprland.org/Configuring/

# Please note not all available settings / options are set here.
# For a full list, see the wiki

# You can split this configuration into multiple files
# Create your files separately and then link them to this file like this:

source= ./colors.conf
################
### MONITORS ###
################

# See https://wiki.hyprland.org/Configuring/Monitors/
monitor = eDP-1, 2880x1800@60, 0x0, 2
monitor = , preferred, auto-up, 1
xwayland {
  # force_zero_scaling = true
}

###################
### MY PROGRAMS ###
###################

# See https://wiki.hyprland.org/Configuring/Keywords/

# Set programs that you use
$terminal = alacritty
$fileManager = nautilus -w
# $menu = ~/.config/rofi/runner.sh
$menu = vicinae toggle
# $clipboard = ~/.config/rofi/clipboard.sh
$clipboard = vicinae deeplink vicinae://launch/clipboard/history?toggle=true
$screenshot = hyprshot-gui
$wallpaper = ~/.config/hypr/scripts/set_bg
$wall_selector = ~/.config/rofi/wall_select/wall_select.sh
$powermenu =  ~/.config/rofi/powermenu/powermenu.sh
$rofiswitch = ~/.config/rofi/rofi_wswitch
#################
### AUTOSTART ###
#################

# Autostart necessary processes (like notifications daemons, status bars, etc.)
# Or execute your favorite apps at launch like this:

# exec-once = gsettings set org.gnome.desktop.interface cursor-theme Adwaita
exec-once = hypridle
# exec-once = hyprpanel
exec-once = waybar
exec-once = swayosd-server
exec-once = wl-paste --type text --watch cliphist store # Stores only text data
exec-once = wl-paste --type image --watch cliphist store # Stores only image data
exec-once = nm-applet --indicator
exec-once = $wallpaper
exec-once = systemctl --user start hyprpolkitagent
exec-once = dunst
# exec-once = qs -c overview
#############################
### ENVIRONMENT VARIABLES ###
#############################

# See https://wiki.hyprland.org/Configuring/Environment-variables/
env = XCURSOR_THEME, Adwaita
env = XCURSOR_SIZE,24
env = HYPRCURSOR_SIZE,24
env = XDG_CURRENT_DESKTOP,Hyprland
env = ELECTRON_OZONE_PLATFORM_HINT,auto
env = QT_QPA_PLATFORMTHEME,qt6ct

#####################
### LOOK AND FEEL ###
#####################

# Refer to https://wiki.hyprland.org/Configuring/Variables/

# https://wiki.hyprland.org/Configuring/Variables/#general
general {
    gaps_in = 2
    gaps_out = 4,4,4,4

    border_size = 2

    # https://wiki.hyprland.org/Configuring/Variables/#variable-types for info about colors
    col.active_border = $primary $tertiary 45deg
    # col.active_border = $color1 $color1 $color3 45deg
    # col.inactive_border = rgba(595959aa)
    col.inactive_border = $surface_container_highest

    # Set to true enable resizing windows by clicking and dragging on borders and gaps
    resize_on_border = false

    # Please see https://wiki.hyprland.org/Configuring/Tearing/ before you turn this on
    allow_tearing = true

    layout = dwindle
    # layout = scrolling
}

# https://wiki.hyprland.org/Configuring/Variables/#decoration
decoration {
    rounding = 7

    # Change transparency of focused and unfocused windows
    active_opacity = 1.0
    inactive_opacity = 1.0

    shadow {
        enabled = false
        range = 4
        render_power = 3
        color = rgba(1a1a1aee)
    }

    # https://wiki.hyprland.org/Configuring/Variables/#blur
    blur {
        enabled = true
        size = 7
        passes = 4
        new_optimizations = true
        # vibrancy = 0.1696

    }
}

cursor {
    no_hardware_cursors = 1
}
# https://wiki.hyprland.org/Configuring/Variables/#animations
# animations {
#     enabled = true
#
#     # Default animations, see https://wiki.hyprland.org/Configuring/Animations/ for more
#
#     bezier = myBezier, 0.05, 0.9, 0.1, 1.05
#     bezier = emphasizedDecel, 0.05, 0.7, 0.1, 1
#     bezier = emphasizedAccel, 0.3, 0, 0.8, 0.15
#
#     animation = windows, 1, 7, myBezier
#     animation = windowsOut, 1, 7, default, popin 80%
#     animation = border, 1, 10, default
#     animation = borderangle, 1, 8, default
#     animation = fade, 1, 7, default
#     animation = workspaces, 1, 6, default
#
#     # animation = specialWorkspaceIn, 1, 2.8, emphasizedDecel, slidevert
#     # animation = specialWorkspaceOut, 1, 1.2, emphasizedAccel, slidevert
# }
animations {
    enabled = true
    # Curves
    bezier = expressiveFastSpatial, 0.42, 1.67, 0.21, 0.90
    bezier = expressiveSlowSpatial, 0.39, 1.29, 0.35, 0.98
    bezier = expressiveDefaultSpatial, 0.38, 1.21, 0.22, 1.00
    bezier = emphasizedDecel, 0.05, 0.7, 0.1, 1
    bezier = emphasizedAccel, 0.3, 0, 0.8, 0.15
    bezier = standardDecel, 0, 0, 0, 1
    bezier = menu_decel, 0.1, 1, 0, 1
    bezier = menu_accel, 0.52, 0.03, 0.72, 0.08
    bezier = stall, 1, -0.1, 0.7, 0.85
    # Configs
    # windows
    animation = windowsIn, 1, 3, emphasizedDecel, popin 80%
    animation = fadeIn, 1, 3, emphasizedDecel
    animation = windowsOut, 1, 2, emphasizedDecel, popin 90%
    animation = fadeOut, 1, 2, emphasizedDecel
    animation = windowsMove, 1, 3, emphasizedDecel, slide
    animation = border, 1, 10, emphasizedDecel
    # layers
    # animation = layersIn, 1, 2.7, emphasizedDecel, popin 93%
    # animation = layersOut, 1, 2.4, menu_accel, popin 94%
    # fade
    # animation = fadeLayersIn, 1, 0.5, menu_decel
    # animation = fadeLayersOut, 1, 2.7, stall
    # workspaces
    animation = workspaces, 1, 7, menu_decel, slide
    ## specialWorkspace
    animation = specialWorkspaceIn, 1, 2.8, emphasizedDecel, slidevert
    animation = specialWorkspaceOut, 1, 1.2, emphasizedAccel, slidevert
    # zoom
    animation = zoomFactor, 1, 3, standardDecel
}

# Ref https://wiki.hyprland.org/Configuring/Workspace-Rules/
# "Smart gaps" / "No gaps when only"
# uncomment all if you wish to use that.
# workspace = w[tv1], gapsout:0, gapsin:0
# workspace = f[1], gapsout:0, gapsin:0
windowrule = match:float 0, match:workspace w[tv1], border_size 0
# windowrulev2 = rounding 0, floating:0, onworkspace:w[tv1]
# windowrulev2 = bordersize 0, floating:0, onworkspace:f[1]
# windowrulev2 = rounding 0, floating:0, onworkspace:f[1]

# See https://wiki.hyprland.org/Configuring/Dwindle-Layout/ for more
dwindle {
    pseudotile = true # Master switch for pseudotiling. Enabling is bound to mainMod + P in the keybinds section below
    preserve_split = true # You probably want this
}

# See https://wiki.hyprland.org/Configuring/Master-Layout/ for more
master {
    new_status = master
}

scrolling {

}
# https://wiki.hyprland.org/Configuring/Variables/#misc
misc {
    force_default_wallpaper = 0 # Set to 0 or 1 to disable the anime mascot wallpapers
    disable_hyprland_logo = true # If true disables the random hyprland logo / anime girl background. :(
	  # vfr = true
    vfr = 1
    vrr = 1
}


#############
### INPUT ###
#############

# https://wiki.hyprland.org/Configuring/Variables/#input
input {
    kb_layout = us
    kb_variant =
    kb_model =
    kb_options =
    kb_rules =

    follow_mouse = 1

    sensitivity = 0 # -1.0 - 1.0, 0 means no modification.
  	accel_profile = flat

    touchpad {
        natural_scroll = true
        scroll_factor = 0.5
    }
}

# https://wiki.hyprland.org/Configuring/Variables/#gestures
gesture= 3, horizontal, workspace

# Example per-device config
# See https://wiki.hyprland.org/Configuring/Keywords/#per-device-input-configs for more
device {
    name = logitech-usb-optical-mouse
    sensitivity = 1
}
device {
	name = elan0522:01-04f3:31c3-touchpad
	sensitivity = 1
}


###################
### KEYBINDINGS ###
###################

# See https://wiki.hyprland.org/Configuring/Keywords/
$mainMod = SUPER # Sets "Windows" key as main modifier

# Example binds, see https://wiki.hyprland.org/Configuring/Binds/ for more
bind = $mainMod, T, exec, $terminal
bind = $mainMod ALT, T, exec, $terminal_alt
bind = $mainMod, Q, killactive,
# bind = $mainMod, M, exit,
bind = $mainMod, E, exec, $fileManager
# bind = $mainMod, B, exec, flatpak run app.zen_browser.zen
bind = $mainMod, B, exec, zen-browser
bind = $mainMod, C, exec, code
bind = $mainMod, Z, exec, zeditor
bind = $mainMod, F, togglefloating,
bind = $mainMod, R, exec, $menu
bind = $mainMod, P, pseudo, # dwindle
bind = $mainMod, J, togglesplit, # dwindle

# Move focus with mainMod + arrow keys
bind = $mainMod, left, movefocus, l
bind = $mainMod, right, movefocus, r
bind = $mainMod, up, movefocus, u
bind = $mainMod, down, movefocus, d

# Switch workspaces with mainMod + [0-9]
bind = $mainMod, 1, workspace, 1
bind = $mainMod, 2, workspace, 2
bind = $mainMod, 3, workspace, 3
bind = $mainMod, 4, workspace, 4
bind = $mainMod, 5, workspace, 5
bind = $mainMod, 6, workspace, 6
bind = $mainMod, 7, workspace, 7
bind = $mainMod, 8, workspace, 8
bind = $mainMod, 9, workspace, 9
bind = $mainMod, 0, workspace, 10

# Move active window to a workspace with mainMod + SHIFT + [0-9]
bind = $mainMod SHIFT, 1, movetoworkspace, 1
bind = $mainMod SHIFT, 2, movetoworkspace, 2
bind = $mainMod SHIFT, 3, movetoworkspace, 3
bind = $mainMod SHIFT, 4, movetoworkspace, 4
bind = $mainMod SHIFT, 5, movetoworkspace, 5
bind = $mainMod SHIFT, 6, movetoworkspace, 6
bind = $mainMod SHIFT, 7, movetoworkspace, 7
bind = $mainMod SHIFT, 8, movetoworkspace, 8
bind = $mainMod SHIFT, 9, movetoworkspace, 9
bind = $mainMod SHIFT, 0, movetoworkspace, 10

# Example special workspace (scratchpad)
bind = $mainMod, S, togglespecialworkspace, magic
bind = $mainMod SHIFT, S, movetoworkspace, special:magic

# Scroll through existing workspaces with mainMod + scroll
bind = $mainMod, mouse_down, workspace, e+1
bind = $mainMod, mouse_up, workspace, e-1

# Move/resize windows with mainMod + LMB/RMB and dragging
bindm = $mainMod, mouse:272, movewindow
bindm = $mainMod, mouse:273, resizewindow

bind = $mainMod ALT, left, movewindow ,l
bind = $mainMod ALT, right, movewindow ,r
bind = $mainMod ALT, up, movewindow, u
bind = $mainMod ALT,  down, movewindow, d


#custom
bind = $mainMod ,W, exec, $wallpaper
bind = $mainMod ALT, W, exec, $wall_selector
bind = $mainMod ,V, exec, $clipboard

bind = $mainMod ,L, exec, loginctl lock-session
bind = ALT, SPACE, exec, $menu
bind = ,Print, exec, $screenshot
bind = $mainMod, X, exec , $powermenu

# bind = $mainMod ALT, H, exec , hyprpanel -q; hyprpanel
# bind = $mainMod, H, exec, hyprpanel toggleWindow bar-0
# bind = $mainMod ALT, H, exec , killall waybar; waybar

bind = $mainMod, H, exec, killall waybar 2>/dev/null || waybar
bind = $mainMod, TAB, workspace, previous

bind = $mainMod, I, exec, killall blueman-manager 2>/dev/null || blueman-manager
bind = $mainMod ALT, I, exec, ~/.config/hypr/scripts/toggle_bluetooth.sh
bind = $mainMod, O, exec, killall nmrs 2>/dev/null || nmrs
bind = $mainMod ALT, O, exec, ~/.config/hypr/scripts/toggle_wifi.sh
# bind = ALT, TAB, exec, $rofiswitch

binde = $mainMod SHIFT, right, resizeactive, 10 0
binde = $mainMod SHIFT, left, resizeactive, -10 0
binde = $mainMod SHIFT, up, resizeactive, 0 -10
binde = $mainMod SHIFT, down, resizeactive, 0 10

bind = $mainMod CTRL, left, movecurrentworkspacetomonitor, l
bind = $mainMod CTRL, right, movecurrentworkspacetomonitor, r
bind = $mainMod CTRL, up, movecurrentworkspacetomonitor, u
bind = $mainMod CTRL, down, movecurrentworkspacetomonitor, d

# suspend on closing lid
# bindl = , switch:Lid Switch, exec, systemctl suspend

# Laptop multimedia keys for volume and LCD brightness
# bindel = ,XF86AudioRaiseVolume, exec, wpctl set-volume @DEFAULT_AUDIO_SINK@ 5%+
# bindel = ,XF86AudioLowerVolume, exec, wpctl set-volume @DEFAULT_AUDIO_SINK@ 5%-
# bindel = ,XF86AudioMute, exec, wpctl set-mute @DEFAULT_AUDIO_SINK@ toggle
bindel = ,XF86AudioMicMute, exec, wpctl set-mute @DEFAULT_AUDIO_SOURCE@ toggle
# bindel = ,XF86MonBrightnessUp, exec, brightnessctl s 5%+
# bindel = ,XF86MonBrightnessDown, exec, brightnessctl s 5%-

bindl = ,XF86AudioMute, exec, swayosd-client --output-volume mute-toggle
bindle =, XF86AudioRaiseVolume, exec, swayosd-client --output-volume raise
bindle =, XF86AudioLowerVolume, exec, swayosd-client --output-volume lower
bindle =, XF86MonBrightnessUp, exec, swayosd-client --brightness +5
bindle =, XF86MonBrightnessDown, exec, swayosd-client --brightness -5

# Requires playerctl
bindl = , XF86AudioNext, exec, playerctl next
bindl = , XF86AudioPause, exec, playerctl play-pause
bindl = , XF86AudioPlay, exec, playerctl play-pause
bindl = , XF86AudioPrev, exec, playerctl previous

##############################
### WINDOWS AND WORKSPACES ###
##############################

# See https://wiki.hyprland.org/Configuring/Window-Rules/ for more
# See https://wiki.hyprland.org/Configuring/Workspace-Rules/ for workspace rules

# common modals
windowrule = match:initial_title ^(Open File)$, float on
windowrule = match:title ^(Choose Files)$, float on
windowrule = match:title ^(Save As)$, float on
windowrule = match:title ^(Confirm to replace files)$, float on
windowrule = match:title ^(File Operation Progress)$, float on
windowrule = match:class ^(xdg-desktop-portal-gtk)$, float on


# Example windowrule v1
# windowrule = float, ^(kitty)$

# Example windowrule v2
# windowrulev2 = float,class:^(kitty)$,title:^(kitty)$

blurls = rofi
layerrule = match:namespace rofi, blur on
layerrule = match:namespace waybar, blur on
layerrule = match:namespace notifications, blur on
# layerrule = blur, swayosd

windowrule = match:class ^(code)$, opacity 0.90
windowrule = match:class ^(dev.zed.Zed)$, opacity 0.93
windowrule = match:class ^(org.gnome.Nautilus)$, opacity 0.80
windowrule = match:class ^(org.mozilla.Thunderbird)$, opacity 0.85
windowrule = match:class ^(org.telegram.desktop)$, opacity 0.85
windowrule = match:class ^(Alacritty)$, opacity 0.80


# Ignore maximize requests from apps. You'll probably like this.
windowrule = match:class .*, suppress_event maximize

# Fix some dragging issues with XWayland
windowrule {
  name = windowrule-16
  no_focus = on
  match:class = ^$
  match:title = ^$
  match:xwayland = 1
  match:float = 1
  match:fullscreen = 0
  match:pin = 0
}


# screenshot border issues
layerrule = match:namespace hyprpicker, no_anim on
layerrule = match:namespace selection, no_anim on


layerrule = blur on, ignore_alpha 0, xray 0, animation slidefade top 90%, match:namespace swaync-control-center
layerrule = blur on, ignore_alpha 0, xray 0, match:namespace swaync-notification-window

# windowrulev2 = opacity 0.80 0.80,class:^(zen)$
#
windowrule {
  name = hyprshot-gui
  match:title = ^(.*Hyprshot.*)$
  size = 200 200
  float = on
  center = true
}
windowrule {
  name = bluetooth-control
  float = on
  size = 400 400
  move = (monitor_w-window_w+55) (monitor_h-window_h-35)
  match:class = ^(blueman-manager)$
  no_max_size = on
}

windowrule {
  name = sound-control
  float = on
  size = 400 800
  move = ((monitor_w*1)-window_w-10) (40)
  match:class = ^(com.saivert.pwvucontrol)$
}

windowrule {
  name = network-control
  float = on
  match:title = ^(.*Network Manager.*)$
}

windowrule {
  name = netrs
  float = on
  size = 100 400
#   move = (monitor_w-window_w) (monitor_h-window_h-35)
  match:class = ^(org.netrs.ui)$
#   no_max_size = on
}

layerrule {
    name = vicinae-blur
    blur = on
    ignore_alpha = 0
    match:namespace = vicinae
}


###############
###  Plugins ###
################
